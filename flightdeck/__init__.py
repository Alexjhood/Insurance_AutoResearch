"""Flight Deck — orchestration report application.

This package namespaces the offline ETL (``flightdeck.etl``), the FastAPI server
(``flightdeck.server``, later phases) and the static export builder
(``flightdeck.export``, later phases). Phase 1 ships only the ETL.
"""
