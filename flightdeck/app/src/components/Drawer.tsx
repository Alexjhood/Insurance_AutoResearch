import { X } from 'lucide-react'; import type { PropsWithChildren } from 'react'; import './primitives.css';
export function Drawer({ title, open, onClose, children }: PropsWithChildren<{title: string; open: boolean; onClose(): void}>) { if (!open) return null; return <div className="drawer-scrim" onMouseDown={e => e.target === e.currentTarget && onClose()}><aside className="drawer" role="dialog" aria-modal="true" aria-label={title}><header className="drawer-header"><h2>{title}</h2><button className="icon-button" onClick={onClose} aria-label="Close"><X /></button></header>{children}</aside></div>; }
